local modeTypes = require(script.Parent.Parent.Parent.types)

return {
    id = "climbing",
    name = "climbing",
    type = "single",
    description = "Who can go the farest?",
} :: modeTypes.info

export type compatibleModes = { string }

export type config = {
    compatibleModes: compatibleModes,
    effects: { string },
}

export type info = {
    id: string,
    name: string,
    description: string,
}

return nil
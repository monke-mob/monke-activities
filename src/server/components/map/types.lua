export type compatibleModes = { string }

export type config = {
    id: string,
    src: Instance,
    compatibleModes: compatibleModes,
    effects: { string },
    data: any,
}

export type info = {
    id: string,
    name: string,
    description: string,
}

return nil
